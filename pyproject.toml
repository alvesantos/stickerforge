[project]
name = "stickerforge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alvesantos", email = "alvesantos.gh@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
stickerforge = "stickerforge:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
